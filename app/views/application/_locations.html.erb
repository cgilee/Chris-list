<div class="list-group col-12 pb-2">
  <% City.all.each do |city| %>
    <%= link_to city.name, city, class:"list-group-item list-group-item list-group-item-action text-truncate text-dark #{active_city(city)}" %>
  <% end %>
</div>
