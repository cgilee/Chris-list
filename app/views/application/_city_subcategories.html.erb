<% subcategories.each do |subcategory| %>
  <%= link_to highlight(subcategory.name, params[:search]), city_subcategory_path(@city, subcategory), class:"list-group-item list-group-item-action text-truncate" %>
<% end %>
